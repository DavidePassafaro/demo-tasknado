# DemoTasknado

To start this demo project, run the following commands:

```bash
npm run init-docker
npm run migrate-docker
```

To run the FE in dev mode:
```bash
npm run init-docker
npm run migrate-docker
npm run stop-fe
npm start
```